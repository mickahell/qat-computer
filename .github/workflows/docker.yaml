name: Docker image

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - '*'

env:
  IMAGE_NAME: "qat-computer"
  DOCKERHUB_USER: "mickahell"
  DOCKERHUB_PASS: ${{ secrets.DOCKERHUB_PASS }}

jobs:
  docker:
    name: Build image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    strategy:
      matrix:
        img_full: ["false", "true"]
    steps:
      - uses: actions/checkout@v3
      - name: Get params
        id: params
        run: |
          # Image version
          if [[ "${{ matrix.img_full }}" == "true" ]]; then
            IMAGE_NAME_VERS="${{ env.IMAGE_NAME }}_full"
          else
            IMAGE_NAME_VERS="${{ env.IMAGE_NAME }}"
          fi

          # Image params
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "version_tag=$(cat VERSION.txt)" >> "$GITHUB_OUTPUT"
            echo "IMAGE_NAME_UP=${{ env.IMAGE_NAME_VERS }}" >> "$GITHUB_OUTPUT"
            echo "REGISTRY_ADD=${{ github.repository_owner }}" >> "$GITHUB_OUTPUT"
          else
            echo "version_tag=$(cat VERSION.txt)-master" >> "$GITHUB_OUTPUT"
            echo "IMAGE_NAME_UP=${{ env.IMAGE_NAME_VERS }}-dev" >> "$GITHUB_OUTPUT"
            echo "REGISTRY_ADD=ghcr.io/${{ github.repository_owner }}" >> "$GITHUB_OUTPUT"
          fi
      - name: Get image name
        id: image-name
        run: |
          IMAGE_ID=${{ steps.params.outputs.REGISTRY_ADD }}/${{ steps.params.outputs.IMAGE_NAME_UP }}
          echo "IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')" >> "$GITHUB_OUTPUT"

      - name: Build image
        run: |
          docker build . \
            --build-arg GITHUB_ACTOR="${{ github.repository_owner }}" \
            --build-arg GITHUB_REPOSITORY="${{ env.GITHUB_REPOSITORY }}" \
            --build-arg GITHUB_REF="${{ steps.params.outputs.version_tag }}" \
            --build-arg FULL="${{ matrix.img_full }}" \
            --file docker/Dockerfile \
            --tag ${{ steps.params.outputs.IMAGE_NAME_UP }}:latest
      - name: Tag image
        run: |
          docker tag ${{ steps.params.outputs.IMAGE_NAME_UP }} \
            ${{ steps.image-name.outputs.IMAGE_ID }}:${{ steps.params.outputs.version_tag }}

      - name: Log in to GitHub registry
        if: github.ref_type == 'branch'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Login to DockerHub registry
        if: github.ref_type == 'tag'
        run: |
          docker login --username=${{ env.DOCKERHUB_USER }} --password=${{ env.DOCKERHUB_PASS }}

      - name: Push image to github registry
        run: |
          docker push ${{ steps.image-name.outputs.IMAGE_ID }} --all-tags